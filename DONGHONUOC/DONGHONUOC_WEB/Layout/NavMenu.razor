<div class="top-row ps-3 navbar navbar-dark d-flex align-items-center">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold mb-0 d-flex align-items-center" href="">
            <img src="images/logo.png" alt="Logo" width="28" height="28" class="me-2 rounded-circle object-fit-cover bg-white p-1" onerror="this.style.display='none'" />
            <span class="lh-1">ĐỒNG HỒ NƯỚC</span>
        </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column pt-3">
        <AuthorizeView>
            <Authorized>
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link rounded-3" href="dashboard" Match="NavLinkMatch.All">
                        <i class="bi bi-speedometer2 me-3 fs-5"></i> Dashboard
                    </NavLink>
                </div>
                <!-- Future Modules
                <div class="nav-item px-3 mb-2">
                    <NavLink class="nav-link rounded-3" href="billing-periods">
                        <i class="bi bi-calendar-week me-3 fs-5"></i> Kỳ Đọc Số
                    </NavLink>
                </div>
                -->
            </Authorized>
        </AuthorizeView>
        
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link rounded-3" href="counter">
                <i class="bi bi-plus-square me-3 fs-5"></i> Counter (Demo)
            </NavLink>
        </div>
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link rounded-3" href="weather">
                <i class="bi bi-cloud-sun me-3 fs-5"></i> Weather (Demo)
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}

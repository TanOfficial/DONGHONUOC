name: doc_so_dong_ho
description: App doc so dong ho nuoc Tan Hoa.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  google_mlkit_text_recognition: ^0.11.0
  permission_handler: ^11.0.1
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  cupertino_icons: ^1.0.2
  image: ^4.7.2
  image_picker: ^1.0.4
  intl: ^0.18.0
  file_picker: ^6.0.0
  csv: ^6.0.0
  share_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # PHẦN THÊM HÌNH ẢNH VÀO ĐÂY
  assets:
    - assets/